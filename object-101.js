let myBook ={
    title: "Harry Potter",
    author:"JK Rowling",
    pagecount:600,
};

console.log(
    `${myBook.title} is by ${myBook.author} and has ${myBook.pagecount} pages`
);

let myData={
    name:"Brender",
    city:"kisumu",
};

console.log(`${myData.name} live in ${myData.city}`);